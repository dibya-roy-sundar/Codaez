name: Schedule Data Refresh

on:
    schedule:
      - cron: '*/2 * * * *'
    workflow_dispatch:

jobs:
    refresh-data:
        runs-on: ubuntu-latest

        steps:
            - name: Trigger data refresh
              run: curl -X GET https://codaez-api.onrender.com/api/v1/details/refreshdata
